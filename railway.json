{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bun install && NITRO_PRESET=node-server bun run build"
  },
  "deploy": {
    "startCommand": "node .output/server/index.mjs"
  }
}
